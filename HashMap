Hashmap is a data structure used to data in a structured format.
in cases where arrays are not enough we use hashmaps
example :
import java.util.HashMap; //importing
class Main {
    public static void main(String[] args) {
        HashMap <Integer,Integer> macha=new HashMap<>();//integer,integer means one is key other is value 
        macha.put(1,3);
        macha.put(2,4);
        System.out.println(macha.get(1));
        
    }
}

get=to retrive key value
put= to keep key and value 
contain=to check if it is present or not 


for (var entry : macha.entrySet()) {
    System.out.println(entry.getKey());
    System.out.println(entry.getValue());
}


code : count number of times the digit is repeating in an array 
import java.util.HashMap;

class Main {
    public static void main(String[] args) {
        HashMap<Integer, Integer> macha = new HashMap<>();
        int[] arr = {1, 4, 5, 2, 3, 1};

        for (int i = 0; i < arr.length; i++) {
            int arraynum = arr[i];
            if (macha.containsKey(arraynum)) {
                int prev = macha.get(arraynum);
                macha.put(arraynum, prev + 1);
            } else {
                macha.put(arraynum, 1);
            }
        }

        // Print all key-value pairs
        for (int a : macha.keySet()) {
            System.out.println(a + " -> " + macha.get(a));
        }
    }
}
///now solve for string array find how many times each string is repeated in a array
import java.util.*;
class Main{
    public static void main(String[] args){
        
        HashMap<String,Integer>macha=new HashMap<>();
        String[] arr={"hi","hello","hey"};
        for(int i=0;i<arr.length;i++){
            String n=arr[i];
            if(macha.containsKey(n)){
                int prev=macha.get(n);
                macha.put(n,prev+1);
                
            }
            else{
                macha.put(n,1);
            }
        }
        for(String a:macha.keySet()){
            System.out.println(a+" "+macha.get(a));
        }
        
    }
}

